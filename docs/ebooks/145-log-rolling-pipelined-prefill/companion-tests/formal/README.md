# Formal Verification Artifacts (TLA+ + Lean)

- Parent README: [../README.md](../README.md)
- Theorem ledger: [THEOREM_LEDGER.md](./THEOREM_LEDGER.md)
- Gnosis compiler boundary: [GNOSIS_COMPILER_BOUNDARY.md](./GNOSIS_COMPILER_BOUNDARY.md)
- Lean package: [lean/README.md](./lean/README.md)
- Betti compiler proofs: [../../../../../../gnosis/GnosisProofs.lean](../../../../../../gnosis/GnosisProofs.lean)

This directory contains machine-checked formal artifacts used to mechanize the paper's strongest quantitative and topological claims.

The in-tree Lean surface now also makes the controller equality boundary explicit: any failure-controller tie involving `keep-multiplicity` resolves to `keep-multiplicity`, a `pay-vent`/`pay-repair` tie resolves to `pay-vent`, and an exact warm-up redline tie chooses topology repair (`expand` or `constrain`) rather than `shed-load`.

The current compiler-side boundary has its own canonical note in [GNOSIS_COMPILER_BOUNDARY.md](./GNOSIS_COMPILER_BOUNDARY.md), so the emitted theorem family, rerun commands, and remaining open measurable-Harris gaps are stated in one place instead of scattered across the manuscript and reviewer notes.

The logic-first Hetero MoA fabric surface now lives here too: four bounded TLA models (`HeteroMoAFabricLowering`, `HeteroMoAFabricCannon`, `HeteroMoAFabricPairing`, `HeteroMoAFabricWaste`) capture mirrored backend-layer lowering, ready-backend and active-layer cardinality bounds, `gnode`-style cannon/helix rotation including zero-lane and zero-layer boundary cases, paired-kernel accept-or-escalate behavior together with disagreement anti-theorems, and Aeon's 10-byte binary stream accounting with exact byte-conservation equivalences before the compiler starts emitting the new primitive.

The dimensional confinement surface is now explicit too: the visible syzygy pair is formalized as the 2-cycle/3D case, the quark tuple as the 3-cycle/4D lift, the lift theorem proves that adding the third cycle increases directed interaction channels from `2` to `6`, and the same file now states the fixed-workload trade directly: more dimension means more channel surface but also more ideal tick cost at fixed chunks, with `55D` carrying more channels than `54D` while costing exactly one more ideal tick. It now also exposes the predictive metric layer: one-step dimension advance adds exactly one ideal tick, adds exactly `2 * (d - 1)` channels, strictly increases the warmup fraction at positive chunk count, and sandwiches both the bandwidth-dilution metric and the channel-minus-tick trade metric.

The ten-mode unification surface now also carries a typed multiple-reality channel reading: the 10-mode field decomposes as `9` interlocking tori plus the sliver, exposes `45` unordered cross-world bridges and `90` directed crossings, totals `55` self-plus-cross channels, splits that surface as `54 + 1` where the `+1` is the monad / void anchor, identifies the same `55` with the in-file Fibonacci-ten and triangular-ten surfaces, proves that the uniform `tenModeKenoma` has constant complement weight `10` with every mode equally peak/Nash, and closes the inverse direction by showing that `55` total channels or `90` directed crossings force the `10`-world case.

## Files

- `ForkRaceFoldC1C4.tla` / `.cfg`: pipeline locality, branch isolation, deterministic fold, bounded termination (C1–C4).
- `QueueingSamplePath.tla` / `.cfg`: finite-trace sample-path Little's Law identity over all work-conserving single-server disciplines in the bounded discrete model.
- `QueueingMultiClassNetwork.tla` / `.cfg`: bounded multi-class open-network conservation law with class-specific routes and finite service-law scenarios under node-local work-conserving dispatch.
- `QueueingStochasticMixture.tla` / `.cfg`: finite-support stochastic multiclass open-network conservation with positive scenario masses and scenario-dependent arrivals, routes, and service realizations.
- `QueueingProbabilisticKernel.tla` / `.cfg`: exact finite-state probabilistic transition kernel for a bounded FIFO queue, with direct distribution-level conservation checks.
- `QueueingProbabilisticNetworkKernel.tla` / `.cfg`: exact finite-state probabilistic multiclass open-network kernel over class-dependent routes, with direct distribution-level conservation checks and the minimal worst-case small-data ramp-up witness.
- `QueueingProbabilisticLargeNetworkKernel.tla` / `.cfg`: larger exact finite-support three-arrival multiclass open-network cube with direct weighted conservation checks over the full 64-branch arrival family.
- `Section7Formulas.tla` / `.cfg`: §7 formulas (Worthington Whip, Speculative Tree, turbulent idle fraction sample, and the frontier-fill / occupancy-deficit complement that reduces to the same idle fraction in the canonical pipeline case).
- `WallaceMetric.tla` / `.cfg`: Wallace metric (`crank`) boundedness/complement on a bounded three-layer frontier, plus the symmetric diamond closed form `2(k-1)/(3k)` and the zero-at-unit-width witness.
- `MultiplexingMonotonicity.tla` / `.cfg`: bounded turbulent-multiplexing monotonicity witness showing fixed useful work plus legal overlap recovery cannot increase the Wallace metric and strictly lowers it when overlap is recovered.
- `FailureEntropy.tla` / `.cfg`: bounded witness for structured failure as live-frontier entropy reduction, including the single-survivor/failure necessity law and the coupled-repair reversal case.
- `FailureFamilies.tla` / `.cfg`: bounded witness for the stronger failure-topology split between branch-isolating failure and contagious failure, including deterministic-fold preservation, zero-repair-debt isolation, and contagious repair-debt forcing.
- `FailureDurability.tla` / `.cfg`: bounded replica witness showing quorum durability under branch-isolating failures with an explicit failure budget, plus eventual return to the fully repaired stable state under weakly fair repair.
- `QuorumReadWrite.tla` / `.cfg`: bounded asynchronous single-key quorum protocol witness with crash, recover, write-delivery, ack, and read steps, checking quorum intersection, ack coverage, and read-after-ack visibility under majority-style quorum assumptions.
- `QuorumAsyncNetwork.tla` / `.cfg`: bounded asynchronous quorum witness with explicit connectivity/partition steps, checking connected-quorum availability, minority-split unavailability, committed-read exactness on connected live quorums, and the observed-read-safety boundary for weak non-quorum reads.
- `QuorumSessionConsistency.tla` / `.cfg`: bounded single-session quorum witness showing committed-read exactness, read-your-writes, and monotonic reads once reads are restricted to committed states (`pendingVersion = 0`).
- `QuorumMultiWriter.tla` / `.cfg`: bounded multi-writer quorum-register witness with globally ordered ballots, concurrent writer starts, highest-ballot commit, and committed-state reads, checking latest-ack exactness, latest-writer tracking, and stale-read exclusion for committed reads.
- `QuorumLinearizability.tla` / `.cfg`: bounded committed-state quorum-register witness for operation-history refinement, checking that observed reads align with the latest completed-write prefix and that speculative completed-history reads mark the boundary.
- `FailureTrilemma.tla` / `.cfg`: bounded impossibility witness for no-free deterministic collapse, showing that a nontrivial fork cannot reach a deterministic single survivor with both zero vent and zero repair debt.
- `FailureComposition.tla` / `.cfg`: bounded composed-pipeline witness showing that the no-free deterministic-collapse boundary persists across multiple stages, so any global deterministic single-survivor collapse must pay vent or repair debt in at least one stage.
- `FailureUniversality.tla` / `.cfg`: bounded arbitrary-depth witness over stage-indexed recovery arrays, showing that the no-free-collapse boundary survives normalization to persistent branch identity, forces a paid stage, imposes the minimum collapse-cost floor `totalVented + totalRepairDebt >= initialLive - 1`, and includes a canonical zero-debt witness that attains that floor exactly.
- `THM-SLIVER-FRACTAL` is currently exposed on the Lean side rather than as a standalone TLA module: `Axioms.lean` carries the general support-preserving coarsening shell, and `InterferenceCoarsening.lean` now adds both a constructive injective-live-support quotient special case with a small app-stage witness and a genuine many-to-one `Phi` aggregation surface that preserves total `λ`, `μ`, `α`, and drift across coarse fibers. `THM-RENORMALIZATION-COARSENING` then closes the one-node measurable renormalization surface by packaging `Φ(G)` as a coarse measurable node on `ℝ` with the same negative drift margin or with the derived total margin coming from summed coarse-node certificates, `THM-RENORMALIZATION-REUSE` closes the recursive one-node reuse layer via `liftToCoarse` / `composeQuotient`, and `THM-RECURSIVE-COARSENING-SYNTHESIS` is therefore the still-open synthesis target, not a missing reuse theorem.
- `RenormalizationCoarsening.tla` / `.cfg`: bounded genuinely many-to-one renormalization witness checking non-injective support collapse, aggregate `λ`/`μ`/`α` preservation, total-drift preservation, collapsed-node rate identification, negative-drift transfer, local coarse-certificate soundness, coarse-margin summation, and transfer of that total coarse margin back to both the fine graph and the collapsed node.
- `RenormalizationComposition.tla` / `.cfg`: bounded recursive-renormalization witness checking that staged quotient reuse preserves intermediate aggregate `λ`/`μ`/`α`/drift data and matches direct composed collapse at the final measurable node.
- `THM-GNOSIS-COUPLED` is currently exposed on the Lean side rather than as a standalone TLA module: `GnosisProofs.lean` packages `coupledArrivalCertificate`, `coupledCertifiedKernel`, and the tethered-kernel stability lemmas showing that nonnegative handoff pressure below downstream drift slack preserves the downstream spectral witness and the pair's geometric stability.
- `HeteroMoAFabricLowering.tla` / `.cfg`: bounded lowering witness for mirrored CPU/GPU/NPU/WASM lane counts, active backend-layer diversity, and top-level laminar collapse shape.
- `HeteroMoAFabricCannon.tla` / `.cfg`: bounded `gnode`-style cannon/helix scheduler witness for pre-arm-before-launch, rotating cursor advance, delayed shadow eligibility, and Aeon 10-byte binary frame headers.
- `HeteroMoAFabricPairing.tla` / `.cfg`: bounded paired-kernel witness showing agreement acceptance, early sufficient-primary shadow skip, disagreement escalation, and single-decision safety.
- `HeteroMoAFabricWaste.tla` / `.cfg`: bounded accounting witness for winner/loser/vent/skipped conservation and monotone framed stream order.
- `FailureController.tla` / `.cfg`: bounded controller witness over the canonical failure actions `keep-multiplicity`, `pay-vent`, and `pay-repair`, showing that the score-minimizing choice is whichever weighted coefficient is smallest against the exact collapse floor `liveBranches - 1`.
- `FailurePareto.tla` / `.cfg`: bounded Pareto witness over the same canonical failure-action family, showing that keep, vent, and repair are pairwise non-dominating when `liveBranches > 1`.
- `StagedExpansion.tla` / `.cfg`: bounded staged-expansion witness showing that, under a positive topology-deficit budget, spending equal added frontier on underfilled shoulders beats naive peak widening in frontier fill and Wallace.
- `WarmupEfficiency.tla` / `.cfg`: bounded warm-up efficiency witness showing exactly when a weighted Wallace reduction is worth an added Buley cost under fixed useful work and legal recovered overlap.
- `DynamicWarmupEfficiency.tla` / `.cfg`: dynamic entropy-creep/active-cooling witness showing bounded overlap and eventual return to laminar flow once the warm-up burden threshold becomes reachable and cooling can clear the boundary layer.
- `SleepDebt.tla` / `.cfg`: bounded wake/recovery/debt witness showing that incomplete recovery leaves positive residual debt and reduced next-cycle capacity, full recovery restores baseline, and debt above threshold admits intrusion-style local venting.
- `SleepDebtScheduleThreshold.tla` / `.cfg`: coarse repeated-cycle threshold witness showing that below-threshold and critical schedules stay debt-free while above-threshold schedules accumulate linear carried debt in cycle count.
- `SleepDebtWeightedThreshold.tla` / `.cfg`: weighted repeated-cycle threshold bridge showing that the integerized `20.2 h` literature boundary stays debt-free at and below threshold while supercritical schedules accumulate linear carried debt.
- `WarmupController.tla` / `.cfg`: bounded controller witness showing the optimal one-step action split between `expand`, `constrain`, and `shed-load`, with the redline at `BurdenScalar < deficitWeight + shedPenalty`.
- `WhipCrossover.tla` / `.cfg`: §7.3 cross-shard correction crossover characterization (`T(S)=ceil(P/S)+(N-1)+C*S`) with finite optimum and strict-over-sharding boundary checks.
- `QuantumDeficit.tla` / `.cfg`: §6.12 speedup = topological deficit identity (Grover-scale sample).
- `BeautyOptimality.tla` / `.cfg`: §6.12 Bu beauty-optimality scaffold (definition, latency/waste monotonicity, Pareto/composition invariants), now paired with a Lean constructive linear/additive special case plus monotone-objective, monotone-profile, comparison-family unique-minimizer, abstract failure-Pareto-frontier universal-floor, non-convex real-objective lifts, a deficit-only boundary witness, a deficit-dominating failure-tax bridge, a failure-tax observable bridge, and strict-profile family instances of both bridge layers; the remaining tracked open target is the unconditional floor theorem that would remove both the monotone-profile and explicit frontier-floor hypotheses.
- `BandGapVoid.tla` / `.cfg`: §6.11 band gaps as voids (`β₂ > 0`) on a finite periodic-energy sample.
- `ProtocolDeficit.tla` / `.cfg`: §8.3 protocol topology deficits (TCP vs QUIC/Aeon Flow).
- `SettlementDeficit.tla` / `.cfg`: §6.12 evidence-table settlement deficit (`Δβ = 2` for T+2 sequential baseline).
- `SchedulerBound.tla` / `.cfg`: conditional scheduler-overhead bound used in §11 performance wording (additive decomposition, bounded overhead, handler-independence under explicit assumptions).
- `lean/Lean/ForkRaceFoldTheorems/Claims.lean`: constructive Lean theorems for quantitative identities, including finite weighted queueing expectation balance, finite-prefix truncation balance, and the linear-additive vs nonlinear-selection correspondence boundary used in §6.12, including the cancellation-target-family impossibility witness for nonlinear folds.
- `lean/Lean/ForkRaceFoldTheorems/MolecularTopology.lean`: constructive molecular-topology theorems for identical Betti-signature homological equivalence, the hole-invariance corollary under homology-preserving deformation, and the broader molecular/pipeline topology surface.
- `lean/Lean/ForkRaceFoldTheorems/Witnesses.lean`: constructive witness catalog for the correspondence boundary, exporting concrete cancellation, partition, and order counterexamples from the Lean package itself.
- `lean/Lean/ForkRaceFoldTheorems/WitnessExport.lean`: Lean-side serializer that turns the witness catalog into the JSON surface consumed by the companion runtime tests and formal-witness artifact writer.
- `lean/Lean/ForkRaceFoldTheorems/AdaptiveWitnesses.lean`: constructive adaptive witness catalog for the bounded two-node raw adaptive `α` closure, exporting the concrete ceiling/drift witness values and theorem refs from the Lean package itself.
- `lean/Lean/ForkRaceFoldTheorems/AdaptiveWitnessExport.lean`: Lean-side serializer that turns the adaptive witness catalog into the JSON surface consumed by the companion runtime adaptive-witness tests and artifact writer.
- `lean/Lean/ForkRaceFoldTheorems/FailureDurability.lean`: constructive bounded-replica theorems showing quorum preservation under an explicit isolated-failure budget and full repair closure once failures are exhausted.
- `lean/Lean/ForkRaceFoldTheorems/ReynoldsBFT.lean`: constructive pipeline/BFT bridge theorems proving the exact `Re < 3/2` quorum-safe boundary, the exact `Re < 2` majority-safe boundary, and the full `mergeAll`/`quorumFold`/`syncRequired` regime partition.
- `lean/Lean/ForkRaceFoldTheorems/PluralistRepublic.lean`: constructive governance theorems showing that plural representation plus positive community context strictly dominates bare one-stream rule with an exact recovered-margin law, and that under civic-capacity cover the resulting community-mediated pluralist republic is optimal in the governance-Bule metric while republican courts keep an exact zero-Bule verdict gate and deliberative consensus is regime-switched by the Reynolds thresholds.
- `lean/Lean/ForkRaceFoldTheorems/QuorumVisibility.lean`: constructive quorum-intersection and read-after-ack theorems plus explicit weak-quorum, contagious-regression, and unfair-repair boundary witnesses.
- `lean/Lean/ForkRaceFoldTheorems/QuorumAsyncNetwork.lean`: constructive connected-quorum theorems for minority-split unavailability and committed-read exactness on connected live quorums, plus explicit stale-read witnesses for weak reads outside quorum.
- `lean/Lean/ForkRaceFoldTheorems/QuorumConsistency.lean`: constructive committed-read exactness, read-your-writes, and monotonic-read lemmas plus explicit pending-read and no-session-floor boundary witnesses.
- `lean/Lean/ForkRaceFoldTheorems/QuorumOrdering.lean`: constructive multi-writer committed-read exactness, latest-writer, and stale-read-exclusion lemmas plus split-connectivity and ballot-collision boundary witnesses.
- `lean/Lean/ForkRaceFoldTheorems/QuorumLinearizability.lean`: constructive latest-completed-write history-refinement lemmas for committed reads, plus the explicit speculative-read boundary witness.
- `lean/Lean/ForkRaceFoldTheorems/FailureEntropy.lean`: constructive Lean theorems for structured failure as live-frontier entropy reduction, including one-survivor/failure necessity and coupled-repair debt monotonicity.
- `lean/Lean/ForkRaceFoldTheorems/FailureFamilies.lean`: constructive Lean theorems for the stronger failure-topology family split, separating branch-isolating failure from contagious failure via deterministic-fold preservation and repair-debt forcing.
- `lean/Lean/ForkRaceFoldTheorems/FailureTrilemma.lean`: constructive Lean theorems for the no-free deterministic-collapse boundary, including the proof that deterministic single-survivor collapse must pay either vented loss or repair debt.
- `lean/Lean/ForkRaceFoldTheorems/FailureComposition.lean`: constructive Lean theorems lifting the no-free deterministic-collapse boundary to aligned stage sequences, proving that any deterministic single-survivor pipeline collapse contains a paid stage.
- `lean/Lean/ForkRaceFoldTheorems/FailureUniversality.lean`: constructive Lean theorems for sparse normalized choice systems and arbitrary-depth recovery trajectories, proving that deterministic fold, zero vent, and zero debt cannot coexist globally at any depth, that deterministic collapse must pay at least the fork-width-minus-one cost floor, and that a branch-isolating witness attains that floor exactly.
- `lean/Lean/ForkRaceFoldTheorems/LandauerBuley.lean`: constructive finite/countable/measurable finite-type thermodynamic calibration layer showing that an equiprobable vented branch carries frontier Shannon entropy in bits, that arbitrary finite-support branch laws sit below the same equiprobable frontier entropy and failure-tax floor, that the finite entropy ceiling is attained exactly by the uniform law and the finite failure-tax ceiling exactly by the uniform law on one- or two-branch supports, that arbitrary Bernoulli binary erasure budgets obey the same boundary, that arbitrary PMFs already admit both a countable-support entropy shell and a matching countable-support heat shell as `ENNReal` truncation/supremum/counting-measure constructions, that arbitrary source PMFs can be pushed through finite/countable measurable observables into a coarse-grained entropy/heat shell on the observable codomain, that the measurable finite-type entropy/heat inequalities and equality characterizations lift through the direct `ENNReal` heat shell, and that the exact `kT ln 2` equality appears precisely on the fair binary fork witness.
- `lean/Lean/ForkRaceFoldTheorems/SleepDebt.lean`: constructive bounded recovery theorems showing that partial recovery leaves positive residual debt and reduced capacity, full recovery restores baseline, repeated truncation can preserve or grow carried debt, and debt above threshold enables intrusion-style local venting.
- `lean/Lean/ForkRaceFoldTheorems/SleepDebtSchedule.lean`: constructive repeated-cycle threshold theorems showing zero carried debt at or below quota and linear debt growth above quota in the bounded schedule model.
- `lean/Lean/ForkRaceFoldTheorems/SleepDebtWeightedSchedule.lean`: constructive weighted repeated-cycle threshold theorems encoding the integerized `20.2 h` literature boundary and linear carried debt growth above it.
- `lean/Lean/ForkRaceFoldTheorems/FailureController.lean`: constructive controller theorems over the canonical failure-action family, proving that the weighted score-minimizing policy picks keep, vent, or repair according to the minimum coefficient on the exact collapse floor.
- `lean/Lean/ForkRaceFoldTheorems/FailurePareto.lean`: constructive Pareto theorems over the canonical failure-action family, proving that keep, vent, and repair all lie on the non-dominated frontier when the collapse gap is positive.
- `lean/Lean/ForkRaceFoldTheorems/BeautyOptimality.lean`: constructive linear/additive beauty theorems, proving deficit/beauty identities, deficit-monotone latency and waste, zero-deficit Pareto optimality in the linear workload model, additive composition, an arbitrary monotone latency/waste objective lift with strict monotone-objective corollaries, reusable generalized-convex and strict generalized-convex cost signatures, a monotone-profile workload lift with generalized-convex, strict-profile, and real-objective corollaries, a comparison-family unique-minimizer theorem, abstract failure-Pareto-frontier universal-floor and real-objective theorems, an explicit deficit-only boundary witness showing that bare deficit bookkeeping does not order latency/waste, an explicit deficit-dominating failure-tax bridge into that frontier layer, an explicit failure-tax observable bridge into that frontier layer, and strict-profile family instances of both bridge layers.
- `lean/Lean/ForkRaceFoldTheorems/Wallace.lean`: constructive Lean theorems for the Wallace/crank metric on a three-layer frontier, including boundedness, complement, zero-fullness equivalence, and the symmetric diamond closed form.
- `lean/Lean/ForkRaceFoldTheorems/Multiplexing.lean`: constructive Lean theorems for turbulent-multiplexing monotonicity under fixed useful work and legal overlap recovery.
- `lean/Lean/ForkRaceFoldTheorems/StagedExpansion.lean`: constructive Lean theorems for staged-expansion dominance over naive peak widening under the same positive deficit-supported budget.
- `lean/Lean/ForkRaceFoldTheorems/WarmupEfficiency.lean`: constructive Lean theorems for the exact warm-up tradeoff threshold between weighted Wallace reduction and added Buley cost.
- `lean/Lean/ForkRaceFoldTheorems/WarmupController.lean`: constructive Lean theorems for the score-minimizing controller that selects `expand`, `constrain`, or `shed-load` from the burden/redline regime.
- `lean/Lean/ForkRaceFoldTheorems/MeasureQueueing.lean`: constructive Lean theorems for infinite weighted queue sums, countably supported stochastic queue laws via `PMF`, measure-theoretic `lintegral` conservation, and monotone truncation-to-limit lifting of queue customer-time balance.
- `lean/Lean/ForkRaceFoldTheorems/QueueBoundary.lean`: constructive Lean bridge packaging the canonical stable `M/M/1` queue as an explicit one-path witness with `β₁ = 0`, capacity `1`, and stationary mean occupancy `λ / (μ - λ)`.
- `lean/Lean/ForkRaceFoldTheorems/QueueStability.lean`: constructive Lean theorems for the stable `M/M/1` stationary occupancy law, its finite first moment, and trajectory-level Cesaro balance for unbounded open-network sample paths.
- `lean/Lean/ForkRaceFoldTheorems/JacksonQueueing.lean`: constructive Lean theorems for a finite-node product-form open-network occupancy law under a stable throughput witness satisfying the Jackson traffic equations, together with in-package witness constructors from the least fixed-point approximation (`constructiveThroughput`) and the resolvent-style spectral formula (`spectralThroughput`) once their side conditions are discharged.
- `lean/Lean/ForkRaceFoldTheorems/JacksonExactClosure.lean`: constructive Lean wrapper for the exact Jackson fixed-point route, proving uniqueness of the real traffic solution under `spectralRadius P < 1`, direct constructive witness construction from any nonnegative stable real fixed point, and exact mean-occupancy / `lintegral` balance corollaries for that route.
- `lean/Lean/ForkRaceFoldTheorems/JacksonRawClosure.lean`: constructive Lean wrapper that packages the raw `maxIncomingRoutingMass`/`minServiceRate` criterion into a named finite Jackson closure with direct network witness construction, mean occupancy, and `lintegral` balance.
- `lean/Lean/ForkRaceFoldTheorems/JacksonFeedForwardClosure.lean`: constructive two-node feed-forward special case showing that the adaptive ceiling kernel has nilpotent routing (`P^2 = 0`), so the closed-form candidate already matches the constructive least fixed point and closes the same mean-occupancy / `lintegral` balance laws directly from raw parameters.
- `lean/Lean/ForkRaceFoldTheorems/JacksonEnvelopeClosure.lean`: constructive Lean wrapper that packages the sharper raw Jackson envelope ladder, exposing the local (`n = 1`), second-order (`n = 2`), and general `throughputEnvelopeApprox n` closure routes into direct network witness construction, mean occupancy, and `lintegral` balance.
- `lean/Lean/ForkRaceFoldTheorems/StateDependentQueueFamilies.lean`: constructive state-dependent queue-family wrappers for vacation, retrial, reneging, and adaptive-routing kernels, all funneled through the shared stationary/terminal balance schemas, plus a raw-parameter two-node adaptive rerouting witness that derives its own ceiling kernel, spectral side conditions, throughput bound, and linear drift witness.
- `lean/Lean/ForkRaceFoldTheorems/InterferenceCoarsening.lean`: constructive graph-quotient surfaces for the interference boundary, deriving positive coarse repair debt from an injective live-support quotient plus deterministic collapse, including a small app-stage witness, exposing a many-to-one `Phi` operator with total `λ`/`μ`/`α`/drift preservation across coarse fibers together with collapsed-node drift transfer, and now closing the one-node measurable renormalization operator `Φ` with exact Foster-Lyapunov drift transfer plus a `CoarseDriftCertificate` surface that aggregates local margins, transfers the derived total margin to the collapsed node, and reuses a supplied certificate across recursive quotient steps through `liftToCoarse` / `composeQuotient`; the next named target is `THM-RECURSIVE-COARSENING-SYNTHESIS`, syntax-driven quotient synthesis and broader recursive certificate reuse.
- `lean/Lean/ForkRaceFoldTheorems/Axioms.lean`: explicit-assumption theorem schemas for global claims, including convergence in the modeled finite class, stronger queue-limit shells, a state-dependent open-network stationary/terminal balance schema whose positive-recurrence and stationary-law layer is derived from explicit kernel bridge, drift, and finite-small-set witnesses, and an adaptive supremum-kernel shell that now derives the expected-Lyapunov ceiling from raw ceiling domination data and supports seven drift routes: an automatically synthesized minimum-slack bottleneck selector, a raw-score normalization route, a positive-part normalization route for arbitrary real scores, an explicit selector-based one-hot decomposition, a normalized nonnegative weighted decomposition, a raw service-slack route, or a raw routing-pressure route.
- `MonoidalCoherence.tla` / `.cfg`: Track Zeta -- pentagon, triangle, and hexagon identities for the fork/race/fold symmetric monoidal category, plus monoidal-category and symmetric-monoidal bundle invariants.
- `lean/Lean/ForkRaceFoldTheorems/MonoidalCoherence.lean`: constructive Lean theorems for pentagon, triangle, hexagon, monoidal-category bundle, symmetric-monoidal bundle, and Mac Lane coherence for the fork/race/fold category with concrete product-type morphisms.
- `GeometricErgodicity.tla` / `.cfg`: Track Delta -- geometric ergodic convergence rates for countable certified kernels, checking TV geometric decay, contraction-rate bounds, mixing-time bounds, and continuous-kernel lift invariants.
- `lean/Lean/ForkRaceFoldTheorems/GeometricErgodicity.lean`: constructive Lean theorems for geometric ergodicity (TV decay at rate r^n), explicit contraction rate (r = 1 - epsilon1 * epsilon2), mixing-time bounds, and continuous Polish-space kernel lift via discrete sub-lattice embeddings.
- `FoldErasure.tla` / `.cfg`: Track Epsilon -- fold-erasure beauty optimality chain from non-injective fold through DPI, Landauer heat, derived coupling, to unconditional beauty floor, plus the injective-fold boundary case.
- `lean/Lean/ForkRaceFoldTheorems/FoldErasure.lean`: constructive Lean theorems proving that non-injective fold erases information, incurs positive Landauer heat, yields a derived (not axiom) ThermodynamicObservableCoupling, and therefore satisfies beauty optimality unconditionally; plus the exact injectivity boundary where the coupling degenerates.

## Current Boundary

- The theorem ledger now spans both the local `formal/lean` package and the Betti compiler proofs in [`open-source/gnosis/GnosisProofs.lean`](../../../../../../gnosis/GnosisProofs.lean).
- The failure/topology side now also has a finite/countable/measurable finite-type thermodynamic calibration layer plus an observable-pushforward shell: `LandauerBuley.lean` proves that equiprobable branch self-information equals frontier Shannon entropy in bits, that every finite branch law on the same nonempty finite support carries entropy at most that equiprobable frontier entropy and therefore at most the existing deterministic-collapse failure-tax floor, that the finite entropy ceiling is attained exactly by the uniform law and the finite failure-tax ceiling exactly by the uniform law on one- or two-branch supports, that every Bernoulli binary branch law obeys the same binary failure-tax bound, that arbitrary PMFs already admit both a countable-support entropy shell and a matching countable-support heat shell as `ENNReal` truncation/supremum/counting-measure formulas, that arbitrary source PMFs can be coarse-grained through finite/countable measurable observables while retaining the same failure-tax and collapse-cost domination on the observable codomain, that finite-effective-support source laws now also satisfy deterministic observable monotonicity so observed entropy/heat stay below the source law and inherit the same frontier/failure-tax/collapse-cost bounds, that the finite entropy/heat inequalities and equality characterizations lift directly through the finite-type `ENNReal` shell, and that the exact `kT ln 2` case is precisely the fair binary fork witness.
- The compiler-side closure is now explicit: `GnosisProofs.lean` mechanizes monoidal `fork` / `race` / `fold` coherence, spectral stability for nilpotent or row-contractive routing kernels, countable small-set recurrence from drift witnesses, a real-state measurable drift helper surface via `derive_gnosis_drift`, a bounded affine measurable `continuousHarris` queue witness over the emitted support kernel, certified geometric stability from spectral plus negative-drift certificates, and the coupled-kernel handoff theorem that spends downstream drift slack without changing the spectral certificate.
- `JacksonQueueing.lean` now exposes two in-package witness paths: `constructiveThroughput` for the least fixed-point approximation and `spectralThroughput` for the closed-form resolvent candidate.
- The package now proves that any nonnegative real fixed point bounds `constructiveThroughput`, so the spectral candidate can certify finiteness and stability for the iterative witness once its own side conditions are shown.
- The current in-package route to a stable throughput witness `α` is explicit: set `α_spec := spectralThroughput = λ (I - P)^{-1}` under `spectralRadius P < 1`, prove `α_spec >= 0`, prove `α_spec < μ`, instantiate `spectralNetworkData`, then transfer those same bounds through `constructiveThroughput_le_spectralThroughput`, `constructiveThroughput_finite_of_spectral`, and `constructiveThroughput_stable_of_spectral` to obtain `constructiveNetworkDataOfSpectral`.
- `JacksonExactClosure.lean` now makes the exact fixed-point route explicit: under `spectralRadius P < 1`, any nonnegative stable real fixed point is unique, equals the constructive least fixed point after `toReal`, and yields the same constructive network witness, mean occupancy, and `lintegral` balance laws. The spectral/resolvent candidate is the canonical corollary of that route.
- `JacksonFeedForwardClosure.lean` now packages a nontrivial raw exact subclass of that route: the bounded two-node feed-forward ceiling witness has nilpotent routing (`P^2 = 0`), so the same closed-form candidate already equals the constructive least fixed point and closes the same mean-occupancy and `lintegral` balance laws directly from raw arrivals, reroute probability, and service rates.
- The full "Jackson Gap" closure path is now readable as one formal pipeline from raw data `(λ, P, μ)`: build the resolvent candidate, prove the traffic equations with `spectralThroughput_fixed_point`, discharge nodewise stability `α_i < μ_i`, then apply the Knaster-Tarski-style dominance bridge `constructiveThroughput_le_of_real_fixed_point` to force finiteness and stability of the constructive witness; the remaining gap is deriving those exact side conditions from raw data automatically, not the transfer once they are known.
- Adaptive routing now has a mechanized comparison layer: `AdaptiveJacksonTrafficData` supports state-indexed routing kernels, any dominating Jackson kernel bounds the adaptive constructive witness, and the `supremumKernel` specialization closes the comparison when the pointwise ceiling remains substochastic and contractive.
- The adaptive drift story is no longer purely schematic: `StateDependentQueueFamilies.TwoNodeAdaptiveRoutingParameters` derives an explicit ceiling kernel, proves the ceiling is strict-row-substochastic and spectrally contractive, bounds the adaptive constructive throughput by a closed-form candidate, and produces a linear drift witness on the bounded adaptive state space.
- The generic adaptive lift still remains honest about its boundary: constructive domination by the supremum or another dominating kernel is proved in `JacksonQueueing.lean`, `Axioms.AdaptiveCeilingDriftSynthesis` now supports a built-in minimum-slack selector route, a raw-score normalization route, a positive-part normalization route for arbitrary real scores, raw service-slack and routing-pressure routes derived from the adaptive data itself, and the earlier explicit selector-based and normalized weighted routes, and the remaining open part is automatic discovery of richer chosen-Lyapunov decompositions for arbitrary adaptive Lyapunov choices beyond those supported forms.
- The Jackson-network gap is narrower now: `JacksonExactClosure.lean` closes the exact fixed-point route once a stable real solution is in hand, `JacksonFeedForwardClosure.lean` packages a nontrivial raw exact subclass with nilpotent two-node feed-forward routing, `JacksonQueueing.lean` extracts `maxExternalArrival` and `maxIncomingRoutingMass` from raw data, organizes the current witness family as a finite-step envelope ladder `throughputEnvelopeApprox n`, with `n = 0` the global envelope, `n = 1` the local nodewise envelope, and `n = 2` the second-order envelope, proves that ladder is descending, adds both scalar residual/error certificates and a routing-shaped node-local residual ladder `throughputResidualApprox n`, supports direct nodewise service bounds on top of that ladder, and also packages the `minServiceRate` corollary as a one-line automatic witness constructor. `JacksonEnvelopeClosure.lean` now turns those sharper ladder stages into named finite-network closure theorems: once any chosen stage already lies below service rates, the same product-form and `lintegral` laws follow constructively at that stage. The remaining open part is deriving exact fixed points or still-sharper arrival/routing certificates directly from raw `(λ, P, μ)` beyond that current exact-witness-plus-ladder/residual scaffold.
- On the compiler side, the next open boundary is no longer finite/countable certified-kernel stability. The current checked surface is `THM-GNOSIS-CONTINUOUS-HARRIS` plus `THM-GNOSIS-GEOMETRY`: for the emitted affine queue family, Betti can already lower the right drift parameters into measurable Harris and geometric-stability certificates.
- Concretely, the compiler can now emit `*_measurable_observable`, `*_measurable_observable_drift`, and `*_measurable_continuous_harris_certified` theorems for the queue-support kernel whenever syntax provides an affine observable family with `0 < driftGap <= observableScale`. What it still needs to synthesize automatically is the measurable small set `C`, the minorization data, and the continuous Lyapunov witness `V(x)` for richer observable families and non-queue measurable kernels, so that fluid backlog, fractional retry mass, or thermal/load coordinates can be certified from the same `VENT` boundaries and ceiling-comparison structure already used in the discrete adaptive shell.
- The open target is continuous syntactic physics: a Betti-to-Lean bridge that lowers arbitrary continuous `.gg` source syntax into a measurable kernel and machine-synthesizes `C`, `V(x)`, and the associated stochastic-limit proof obligations directly from program structure. The bounded affine queue witness closes the first queue-family continuous-Harris step; it does not yet make Betti a universal continuous-syntax physics oracle.
- The open part of the thermodynamic story is explicit too: the current Landauer bridge now closes the finite-support inequality/equality characterizations, the countable-support entropy/heat shells, the direct measurable finite-type entropy/heat shell, the finite/countable observable pushforward shell for arbitrary source PMFs, and finite-effective-support observable monotonicity back to the source law, but an intrinsic measurable erasure theorem for arbitrary branch laws without first choosing a coarse observable partition, together with the matching measurable collapse-cost comparison and a support-free observable monotonicity theorem on that intrinsic surface, is still open.
- On the beauty side, the constructive surface now reaches arbitrary componentwise monotone nonlinear objectives, arbitrary real monotone objectives, and monotone generalized-convex costs over deficit-monotone latency/waste profiles, a comparison-family unique-minimizer theorem, abstract failure-Pareto-frontier zero-deficit floor theorems under explicit measure-side bounds, an explicit deficit-only boundary witness showing that bare deficit bookkeeping is insufficient, an explicit deficit-dominating failure-tax bridge, an explicit failure-tax observable bridge that packages the remaining interface from collapse-cost/entropy tax into latency/waste floor inequalities, and strict-profile family instances of both bridge layers. The tracked open target is still the unconditional floor theorem: the current package now has the abstract bridge stack and concrete beauty-side inhabitants of it, but it does not yet derive that bridge from deficit bookkeeping or instantiate it automatically from the current failure-tax theorems. However, `THM-BEAUTY-ERASURE-SUFFICIENT` now narrows the gap significantly: for any system whose fold is non-injective (many-to-one), the coupling is derived from fold erasure as a theorem (`THM-ERASURE-COUPLING`), making beauty optimality unconditional. `THM-FOLD-INJECTIVITY-BOUNDARY` identifies the exact cutoff: only injective folds require the external axiom.
- **Monoidal coherence** is now mechanized: `THM-PENTAGON`, `THM-TRIANGLE`, and `THM-HEXAGON` prove the three generators of Mac Lane coherence, `THM-MONOIDAL-CATEGORY` bundles them into a monoidal category proof, `THM-SYMMETRIC-MONOIDAL` adds the braiding, and `THM-COHERENCE` invokes Mac Lane's theorem for full coherence. The §3.6 claim that fork/race/fold form a symmetric monoidal category is now proved rather than sketched.
- **Geometric ergodic convergence rates** are now mechanized: `THM-GEOMETRIC-ERGODICITY-DISCRETE` proves TV decay at rate r^n, `THM-GEOMETRIC-ERGODICITY-RATE` gives the explicit rate formula r = 1 - epsilon1 * epsilon2, `THM-MIXING-TIME-BOUND` gives a logarithmic mixing-time bound, and `THM-CONTINUOUS-ERGODICITY-LIFT` extends these from discrete to continuous Polish-space kernels. The Harris recurrence machinery now quantifies convergence speed, not just existence.
- `Axioms.lean` remains the location for the stronger state-dependent stability, limit, and convergence shells that still need concrete model instantiations, but the generic state-dependent queue shell no longer carries free `positiveRecurrent` or `stationaryLawExists` props: those are derived internally from explicit kernel witnesses.
- `Axioms.lean` also now carries the honest app-as-node coarsening shell for interference: the current theorem says a support-preserving quotient cannot make contagious fine-scale collapse free. `InterferenceCoarsening.lean` now adds the constructive injective-live-support special case together with many-to-one `Phi` aggregation lemmas that preserve total `λ`, `μ`, `α`, and drift, it now closes a one-node measurable renormalization theorem via `Φ`, and it packages local coarse-node drift certificates into a certified total renormalization margin that can itself be transferred onto the collapsed measurable node and reused across recursive quotient steps. The next system-scale target is now indexed as `THM-RECURSIVE-COARSENING-SYNTHESIS`: derive the quotient operator, aggregate the coarse graph interface, and synthesize reusable nodewise coarse-margin witnesses from syntax or local proofs so verified subgraphs can be reused recursively as primitive nodes without hand-constructing the quotient or the outer certificate calculus.

## Derived Vocabulary

- The formal subtree now carries theorem-indexed definitions for `branch mass`, `collapse law`, `interference pattern`, `vented loss`, `optionality`, `structured ambiguity processor`, `Harrigan Margin`, `Harrigan Horizon`, `volatility budget`, `dynamic Harrigan Horizon`, `Harrigan Cascade`, `coherence field`, `coherence bandwidth`, `culture field`, `cultural memory`, `Brainwash Principle`, `forced amnesia`, `cover-space corollary cracking`, `offline-risk negative control`, `recovery-trust surface`, `witness ancestry`, `ancestry-preserving reconciliation`, and `void-preserving fold`; see [THEOREM_LEDGER.md](./THEOREM_LEDGER.md).
- These are documentation-layer summaries over existing theorem families, not new Lean/TLA objects.
- The mapping is explicit: failure theorems supply `branch mass`, no-free-collapse, and collapse-cost floor; the Landauer bridge calibrates that floor against equiprobable frontier erasure, arbitrary finite-support branch-law entropy, Bernoulli binary erasure/heat, the countable-support entropy/heat shells, and the direct finite-type measurable entropy/heat lifts; monoidal and correspondence theorems supply `collapse law` and collapse-truthfulness boundaries; cancellation/partition/order witnesses supply `interference pattern`; vent/debt theorems supply `vented loss`.
- The boundary is also explicit: this vocabulary does not by itself certify race-winner correctness or discharge measurable-limit / Harris-style claims without the witness structures already listed in the ledger.

## Run

From `companion-tests/`:

```bash
bun run test:formal:parser
bun run test:formal:lean
bun run test:formal:gnosis
bun run test:formal:witnesses
bun run test:formal:adaptive-witnesses
bun run test:formal
```

`test:formal:parser` validates all `.tla/.cfg` artifacts and inspects the Lean project through the self-hosted `aeon-logic` parser/sandbox surface.
`test:formal:lean` builds the in-tree `formal/lean` theorem modules through `aeon-logic`'s `runLeanSandbox`, which in turn requires a successful `lake build`.
`test:formal:gnosis` wraps `lake build GnosisProofs` in the shared `open-source/gnosis` workspace and reruns the compiler-side emitted-kernel theorem surface.
`test:formal:witnesses` builds the Lean witness module, exports the constructive correspondence-boundary counterexamples, and refreshes the runtime-facing witness artifact.
`test:formal:adaptive-witnesses` builds the adaptive witness module, exports the concrete bounded two-node adaptive `α` witness, and refreshes the runtime-facing adaptive witness artifact.
`test:formal` runs parser preflight, the in-tree Lean build, the shared Gnosis theorem build, and TLC model checking.

## Focused Commands

```bash
# Bounded sleep-debt theorem family
cd /Users/buley/Documents/Code/emotions/open-source/aeon/docs/ebooks/145-log-rolling-pipelined-prefill/companion-tests
bun run test:sleep-debt
AEON_LEAN_BUILD_TARGETS=ForkRaceFoldTheorems.SleepDebt bun ./scripts/run-lean-theorems.ts
java -cp formal/.tools/tla2tools.jar tlc2.TLC -deadlock -config formal/SleepDebt.cfg formal/SleepDebt.tla

# Coarse repeated-cycle sleep-schedule threshold family
bun run test:sleep-debt-threshold
AEON_LEAN_BUILD_TARGETS=ForkRaceFoldTheorems.SleepDebtSchedule bun ./scripts/run-lean-theorems.ts
java -cp formal/.tools/tla2tools.jar tlc2.TLC -deadlock -config formal/SleepDebtScheduleThreshold.cfg formal/SleepDebtScheduleThreshold.tla

# Weighted repeated-cycle sleep-schedule threshold family
bun run test:sleep-debt-weighted
AEON_LEAN_BUILD_TARGETS=ForkRaceFoldTheorems.SleepDebtWeightedSchedule bun ./scripts/run-lean-theorems.ts
java -cp formal/.tools/tla2tools.jar tlc2.TLC -deadlock -config formal/SleepDebtWeightedThreshold.cfg formal/SleepDebtWeightedThreshold.tla
```

Use these when you want the smallest mechanized rerun surface for `THM-SLEEP-DEBT`, `THM-SLEEP-SCHEDULE-THRESHOLD`, or `THM-SLEEP-WEIGHTED-THRESHOLD` rather than the entire formal package.
