# Lean Source

- Parent README: [../README.md](../README.md)
- Gnosis compiler boundary: [../../GNOSIS_COMPILER_BOUNDARY.md](../../GNOSIS_COMPILER_BOUNDARY.md)
- Child README: [ForkRaceFoldTheorems/README.md](./ForkRaceFoldTheorems/README.md)

Lean source modules for the in-tree formal theorem package. The compiler-emitted kernel theorems referenced from the ledger are split across this directory and the shared `open-source/gnosis/GnosisProofs.lean` workspace, which carries the emitted-kernel spectral/recurrence/geometric/continuous-Harris/coupled theorem family. For the canonical summary of that compiler-side theorem surface and its still-open measurable-Harris gaps, see [../../GNOSIS_COMPILER_BOUNDARY.md](../../GNOSIS_COMPILER_BOUNDARY.md).

The newest finite-topology bridge is `ForkRaceFoldTheorems/TopologicalMismatchAdequacy.lean`, which packages the honest realizability cutoff for the current `Δβ` model: `Δβ ≤ 0` is exactly the lossless-realization regime on positive stream budgets, `Δβ = 0` is the tight lossless regime, and `Δβ > 0` forces collisions and information loss under every realization.
That bridge now has a repo-native transport wrapper too in `ForkRaceFoldTheorems/AeonFluxSiteAdequacy.lean`, which states the same theorem family for positive-stream-budget Aeon Flux sites and matches the compiler-side `WallingtonRotation` witness emitter.

The replacement-facing shell has widened too. `ForkRaceFoldTheorems/GaugeRepresentationClosure.lean`, `ForkRaceFoldTheorems/YukawaMassGenerationClosure.lean`, and `ForkRaceFoldTheorems/GaugeMassShellClosure.lean` now close the explicit gauge/Higgs/Yukawa scaffold; `ForkRaceFoldTheorems/PerturbativeBetaClosure.lean`, `ForkRaceFoldTheorems/KinematicScatteringClosure.lean`, `ForkRaceFoldTheorems/GravitationalDynamicsClosure.lean`, `ForkRaceFoldTheorems/ResidualStrongForceClosure.lean`, `ForkRaceFoldTheorems/MeasuredFlavorClosure.lean`, `ForkRaceFoldTheorems/LocalGravityLaw.lean`, `ForkRaceFoldTheorems/ResidualNuclearForce.lean`, `ForkRaceFoldTheorems/PerturbativeScatteringClosure.lean`, `ForkRaceFoldTheorems/DarkSectorForceLawClosure.lean`, `ForkRaceFoldTheorems/UnifiedReplacementClosure.lean`, and `ForkRaceFoldTheorems/StandardModelReplacementClosure.lean` now carry the simplified and stronger Chapter 17 replacement sweeps directly in-tree.

The latest in-tree arithmetic expansion is in `ForkRaceFoldTheorems/HeteroMoAFabric.lean`: it now includes exact four-backend cardinality bounds, zero-lane/zero-layer scheduler boundary witnesses, disagreement anti-theorems for paired kernels, and binary-frame monotonicity/injectivity lemmas alongside the earlier lane-count and laminar-height results.
The newest Chapter 17 shape expansion is in `ForkRaceFoldTheorems/DeceptaconVariants.lean`: it makes the `VOID` contract explicit in Lean, separates legacy `projection` / `search` operations from named BATNA/WATNA branches, types `DualVoidDeceptacon` with `voidToggle`, and packages `TridentDeceptacon` with the live streaming-head branch plus a two-rotation meta-LAMINAR head pipeline surface.

The dimensional geometry surface in `ForkRaceFoldTheorems/DimensionalConfinement.lean` now also makes the syzygy/quark lift explicit: the 2-cycle syzygy is the visible 3D case, the 3-cycle quark tuple is the 4D lift, the exact arithmetic lemmas show the lift increases directed channels from `2` to `6`, and the same module now proves the fixed-chunk trade that each added dimension increases ideal tick cost, with `55D` one ideal tick more expensive than `54D` while also exposing a larger channel surface. It now additionally packages the predictor layer: exact one-step tick gain, exact one-step channel gain, strict warmup-fraction increase, and sandwich bounds for both bandwidth dilution and the channel-minus-tick trade metric.

The projection layer now also has an explicit celestial-shadow surface in `ForkRaceFoldTheorems/CelestialShadows.lean`: rather than claiming a universal planet theorem, it introduces typed star-like / planet-like / Saturn-like shadow classifications and then gives constructive `54D` witnesses showing that the already-certified `2756`-channel budget supports both a radial-dominant stellar reading and a ring-dominant Saturn reading, with the Saturn witness tying its equatorial surplus exactly to the existing `52`-tick ramp-up count.

That surface now has a general astrophysical projection calculus in `ForkRaceFoldTheorems/AstrophysicalProjection.lean`: explicit radial/equatorial dominance assumptions imply star-like, planet-like, or Saturn-like readings; swapping the radial and equatorial partitions preserves budget, dimension, and halo bookkeeping while flipping morphology; and the anti-theorem surface makes the non-uniqueness boundary explicit by proving that a fixed visible budget does not determine a unique morphology.

It now also composes with the cosmic observation boundary in `ForkRaceFoldTheorems/CelestialKnowability.lean`: the CMB surface can be visible, the pre-CMB epoch can remain hidden, the total observer gap can stay positive, and yet the same visible budget still fails to determine a unique celestial morphology.

The inverse-problem layer is now explicit too in `ForkRaceFoldTheorems/CelestialIdentifiability.lean`: a minimal `(dimension, halo, visibleBudget)` signature is preserved by swapping radial and equatorial partitions, the signed morphology bias flips sign under that swap, and the resulting anti-theorem shows that this observable signature is not injective on morphology.

That inverse-problem surface now has an explicit impossibility layer in `ForkRaceFoldTheorems/CelestialClassifierBarrier.lean`: any classifier that only reads the observable signature is forced to agree on the `54D` stellar/Saturn witness pair, so the package now closes honest anti-theorems ruling out signature-only recovery of Saturn-likeness, star-likeness, and positive morphology bias.

The celestial branch now also carries a predictive orbital layer in `ForkRaceFoldTheorems/CelestialOrbitPrediction.lean`: ring-dominance gain and halo-backed control define a lower/upper orbital window, the `54D` Saturn witness gets the exact window `[52, 105]`, and a bounded Skyrms encoding proves that halo-matched gain reaches a literal orbital-equilibrium nadir for that witness while the same observable signature still fails to determine the orbital window uniquely.

That orbital surface now has a concrete planet taxonomy in `ForkRaceFoldTheorems/CelestialPlanetTaxonomy.lean`: the single certified `54D` budget supports halo-locked, diffuse-ring, and super-ring planet families simultaneously, with strictly ordered lower and upper orbital windows, so the package now predicts distinct ringed planet classes instead of only proving that one Saturn witness exists.

The lower-dimensional edge is now explicit in `ForkRaceFoldTheorems/CelestialOffByOneTaxonomy.lean`: the existing off-by-one law `wallingtonDimension stages = stages + 1` is used to place photon-like objects at the one-cycle floor and Earth-like compact rocky objects at the two-cycle / ambient `3D` floor, separating low-dimensional non-gas worlds from the higher-dimensional gas/ring taxonomy.

That same off-by-one law now closes the universe-shape boundary in `ForkRaceFoldTheorems/UniverseShapeByDimension.lean`: the visible ambient `3D` slice is proved to be a `2`-torus with `β₁ = 2`, the Earth-like floor witness is used as the `3D` anchor, and the matching anti-theorem makes the scope explicit by proving that ambient dimensions `≥ 4` are not `2`-torus slices.

That taxonomy now has an executable prediction layer in `ForkRaceFoldTheorems/CelestialGainControlPrediction.lean`: lower/upper orbital windows are repackaged as gain/control statistics, ring taxa are classified directly from those statistics, and concrete photon-like, compact rocky, diffuse-ring, halo-locked, and super-ring packets are all predicted constructively.

The next bridge is `ForkRaceFoldTheorems/CelestialSurveySearch.lean`: the same math is lifted onto observed catalog packets `(ambientDimension, visibleBudget, gain, control, skyrmsLocation)`, yielding an executable candidate filter and a small formal demo of a math-only SETI-style search that retains candidate packets and rejects unresolved noise.

That survey surface now has a proved ranking layer in `ForkRaceFoldTheorems/CelestialNaturalDivision.lean`: the orbital spread/control statistic is re-read as the natural-division size, the floor packets land at Barbelo, the `54D` ring packets land at exact Pleroma offsets, and the same ranking lifts exactly into picolorenzos.

The higher-order counting surface in `ForkRaceFoldTheorems/TenModeUnification.lean` now also exposes the multiple-reality channel model: `9` interlocking tori plus the sliver recover the 10-mode field, with `45` unordered bridges, `90` directed crossings, and a `55 = 54 + 1` channel split where the `+1` is the monad / void anchor; that same `55` is proved equal to both the triangular-ten and Fibonacci-ten counts already present in the module, the uniform `tenModeKenoma` is shown to keep every complement weight at `10` with every mode equally peak/Nash, and the new inverse theorems show that both the `55`-channel surface and the `90`-crossing surface pick out the `10`-world case exactly.

The next diagnostic layer is `ForkRaceFoldTheorems/FibonacciSizeSurface.lean`: instead of assuming that all certified scales are Fibonacci-valued, it tests the current size statistics directly. That test passes for proton cycle count and the low-dimensional floor packets, fails for the present ring-planet natural-division sizes, and leaves black-hole physical size open because the current singularity surface only exposes a touchpoint-count statistic.

The next floor-localization layer is `ForkRaceFoldTheorems/PhotonSliverFrontier.lean`: it packages the photon-like witness as the current `2D` zero-budget positioning floor with gain/control `(0, Barbelo)`, Barbelo natural division, and one-Barbelo picolorenzo size, while also proving that the Earth-like compact-rocky floor shares the same sliver-sized control and size statistic. The current reading is therefore nested rather than flat: both the ambient layer and the embedded packet layer are Wallington rotations, with only two certified nesting levels so far.

The next nesting layer is `ForkRaceFoldTheorems/NestedWallingtonTower.lean`: it proves the immediate off-by-one Wallington nesting law for every positive ambient dimension, packages the currently explicit local ladder as photon floor, rocky floor, and quark lift, proves that the localized photon packet certifies exactly two local levels while the explicit ladder has depth `3`, and separates that from the ten-mode envelope, which closes finitely at depth `10` without yet giving a theorem that every intermediate rung is closed.

The next admissibility layer is `ForkRaceFoldTheorems/WallingtonSurfaceAdmissibility.lean`: it makes "best admissible surface" precise by defining admissibility as exact ambient closure `wallingtonDimension k = n`, then proves that the unique admissible stage count is `k = n - 1`, with smaller candidates undershooting and larger ones overshooting. The concrete visible corollary is the expected one: the `3D` slice takes the `2`-torus as its best admissible surface.

The next atlas layer is `ForkRaceFoldTheorems/KnowableUniverseMap.lean`: it packages the existing finite observable-epoch ceiling, positive visibility gap, CMB cutoff, best ambient visible surface, nested local Wallington ladder, packet survey filter, black-hole monad recovery theorem, and the base `1 -> 0` Buleyean logic/sliver bridge into one bounded knowable-universe map. The result is a certified atlas of the knowable surface, not an unrestricted theorem about everything that exists.

The next shared-interface layer is `ForkRaceFoldTheorems/AtlasCoordinateContract.lean`: it fixes one canonical coordinate contract across that atlas, with primary navigation tuple `(time, dimension, space)`, secondary Wallington-local placement tuple `(x, y, z)`, and signed state scalar `-1 | 0 | 1` for `void | boundary | live`. The off-by-one geometry then proves the local collapse `x = y` and `z = y + 1`, and the file packages canonical witnesses for the Bythos void edge, the visible boundary slice, and the first live one-cycle floor. It now also makes the Buleyean/sliver bridge explicit: the atlas refines Buleyean logic by keeping sign on top of the one-Barbelo magnitude, so `boundary` is ground while `void` and `live` are opposite-oriented slivers. The same file now proves the first genuine atlas transfer theorem too: shared signature forces shared local geometry and shared logic.

The next bounded closure layer is `ForkRaceFoldTheorems/StrongerCosmologyClosure.lean`: it packages the named `BythosScale.lean` hierarchy as the current discrete physical radius law, packages positive fold erasure from `MolecularTopology.lean` as the current mass-energy law, quantifies the full ten-rung ambient Wallington closure over `1..10`, and re-closes the explicit black-hole cosmology as `9` torus carriers plus `1` monad touchpoint with unique boundary recovery. The scope is still finite and model-relative, but the stronger program is no longer left as loose prose.

The adjacent matter-facing closure is now explicit too in `ForkRaceFoldTheorems/MatterExplanationClosure.lean`: it packages the Demiurge distinction between massless ground and positive excited cost, identifies the proton with the confined `3`-cycle / `4D` rung, promotes positive erasure to the current mass-energy budget, and packages positive fold energy as topological backreaction in the self-referential gravity shell. That closes the current answer to why matter has mass without overselling it as a full numeric rest-mass derivation.

The next proton-specific shell is now explicit too in `ForkRaceFoldTheorems/ProtonRestMassCandidate.lean`: it defines a continuous candidate formula for proton rest mass, proves the candidate is affine-linear and monotone in confinement radius, and closes one exact rational calibration witness at `938.272 MeV`. `ForkRaceFoldTheorems/ProtonCalibrationBoundary.lean` then closes the boundary around that fit: the current shell admits distinct exact residual and confinement calibrations. `ForkRaceFoldTheorems/ProtonQuantizedUniqueness.lean` adds the strongest honest positive closure on top of that: with the current QCD/radius anchors plus integer fold quanta and a sub-unit sliver remainder, the calibrated proton witness is unique. `ForkRaceFoldTheorems/ParticleTheoryCoreClosure.lean` packages the next bounded particle-theory layer by composing the constructive particle witness, the confinement shell, the boson shell, the proton matter rung, and the explicit uniqueness shell into one theorem surface. `ForkRaceFoldTheorems/FermionChargeClosure.lean` then closes the adjacent three-generation/charge-table shell, and `ForkRaceFoldTheorems/FlavorMixingClosure.lean`, `ForkRaceFoldTheorems/RunningCouplingClosure.lean`, `ForkRaceFoldTheorems/ElectroweakScatteringClosure.lean`, and `ForkRaceFoldTheorems/StandardModelShellClosure.lean` extend that bounded surface to CKM/PMNS-style normalized mixing kernels, a discrete running-coupling ladder, named conservation-law scattering channels, and one master bounded Standard-Model-style shell theorem. The next bounded pass is now formalized too: `ForkRaceFoldTheorems/FlavorPhaseClosure.lean`, `ForkRaceFoldTheorems/AnomalyCancellationClosure.lean`, `ForkRaceFoldTheorems/ContinuousBetaFunctionClosure.lean`, `ForkRaceFoldTheorems/ScatteringAmplitudeClosure.lean`, and `ForkRaceFoldTheorems/ExtendedStandardModelShellClosure.lean` add quarter-turn phase assignments, one-generation anomaly cancellation, continuous sign-correct beta-function flows, bounded amplitude/cross-section proxies, and one master extended Standard-Model-style shell. So the package now contains both the explicit non-uniqueness boundary and the strongest bounded Standard-Model-style shell the repo has had, while still keeping measured complex phases, perturbative beta coefficients, collider amplitudes, and any literal full-Standard-Model-replacement reading outside the theorem boundary.

The in-tree package now also includes an explicit controller tie-breaking surface: equality in the failure controller resolves lexicographically (`keepMultiplicity`, then `payVent`, then `payRepair`), and exact warm-up redline ties choose topology repair rather than `shed-load`.

The Reynolds-BFT bridge is now exact as well: `Re < 3/2` iff quorum-safe, `Re < 2` iff majority-safe, and `classifyRegime` partitions the `mergeAll`, `quorumFold`, and `syncRequired` bands exactly.

The governance surface now also packages a community-mediated pluralist republic: plural civic capacity strictly beats one-stream rule with an exact arithmetic gap, and under civic-capacity cover the polity reaches the global minimum of the governance-Bule metric while courts expose an exact `β₁ = 0` verdict gate and deliberative assemblies surface the exact Reynolds consensus regime.

The cosmology-facing arithmetic surface now also includes `ForkRaceFoldTheorems/CosmicOptimalDelta.lean`, `ForkRaceFoldTheorems/CMBVisibilityBoundary.lean`, and `ForkRaceFoldTheorems/KnowabilitySplit.lean`: the compiler-side optimal delta, the photon last-scattering boundary, and their composition showing that zero exploration still leaves a positive observer gap together with a pre-CMB hidden epoch.

That same cosmology surface now has an explicit observable-finitude wrapper in `ForkRaceFoldTheorems/ObservableUniverseFinite.lean`: the named `CosmicProjection` epochs are all finite and share one finite `maxObservable` ceiling, while unrestricted total-universe finitude remains outside the claim.

Primary entrypoint:

- `ForkRaceFoldTheorems.lean`
- `open-source/gnosis/GnosisProofs.lean`: shared compiler-side theorem workspace for emitted Gnosis kernels, including bounded affine measurable `continuousHarris` and bounded coupled-kernel tethering.
- `ForkRaceFoldTheorems/README.md`: constructive claims, the linear/additive beauty-optimality special case with strict corollaries, arbitrary monotone latency/waste objective lift, arbitrary deficit-monotone latency/waste profile lift with strict generalized-convex and real-objective corollaries, the topological-mismatch adequacy theorem family for finite realizations, the Aeon Flux site adequacy wrapper for compiler-readable FlowFrame transport sites, an indexed comparison-family unique-minimizer lift for strict generalized-convex beauty costs, abstract failure-Pareto-frontier universal-floor and real-objective lifts under explicit measure-side bounds, an explicit deficit-only beauty boundary witness, an explicit deficit-dominating failure-tax bridge into that frontier layer, an explicit failure-tax observable bridge into that frontier layer, strict-profile family instances of both bridge layers, failure-topology entropy, failure-family-split, failure-trilemma, failure-composition, failure-universality, minimum-collapse-cost, exact-floor-attainability, controller tie-breaking at equality, exact Reynolds-BFT regime classification, community-mediated pluralist-republic governance theorems with exact strict-dominance and governance-Bule optimality, the constructive/coarsening interference boundary plus many-to-one renormalization closure, the additive coarse-margin certificate route, the one-node measurable renormalization operator `Φ`, and the recursive one-node reuse layer via `liftToCoarse` / `composeQuotient`, the bounded sleep-debt witness, the coarse repeated-cycle schedule-threshold witness, and the weighted `20.2 h` threshold bridge, quorum visibility/session/multi-writer ordering/history-refinement theorems, multiplexing/Wallace/staged-expansion/warm-up-efficiency/controller theorems, queueing measure-limit and stability theorems including the exact Jackson fixed-point closure, the raw Jackson closure, the two-node feed-forward exact subclass, and the sharper envelope-ladder closure, the Lean-originated witness/export bridges, and higher-level theorem schemas, including the correspondence-boundary and cancellation-family theorems.
