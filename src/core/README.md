# Core Module

Parent: [../README.md](../README.md)

This directory holds Aeon's shared type surfaces and internal primitives,
including:

- generic library types in `types.ts`
- `AeonEventEmitter`, the dependency-free event surface used by Aeon's
  control-plane modules
